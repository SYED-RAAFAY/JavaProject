<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tvacstudio.GroT">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".grotra"></activity>
        <activity android:name=".Todo" />
        <activity android:name=".Bills" />
        <activity android:name=".HomeMaintain" />
        <activity android:name=".ManageGroceries" />
        <activity android:name=".Tracker" />
        <activity android:name=".DashBoard" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".OtpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>